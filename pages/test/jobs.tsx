import React from 'react';
import { Container } from '@material-ui/core';
import Header from '../../components/header';

const jobs: React.FC = () => (
		<div>
			<Header/>
		</div>
	)

export default jobs
