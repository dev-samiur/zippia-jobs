import React from 'react';
import { Container } from '@material-ui/core';
import Header from '../../components/header';

const jobs: React.FC = () => (
		<div style={{margin: -8}}>
			<Header/>
		</div>
	)

export default jobs
